package com.supreme.priceintelligence.ui.theme

import androidx.compose.ui.graphics.Color

// ---- "Supreme Dark" base palette ----
val Bg = Color(0xFF0B0F14)          // Deep obsidian app background
val Surface = Color(0xFF1E2128)     // Dark charcoal card surface
val SurfaceAlt = Color(0xFF313540)  // Subtle gray — borders & dividers

// Primary brand
val Brand = Color(0xFF10B981)       // Neon emerald — active icons, scanner, lowest price
val BrandLight = Color(0xFF123626)  // Deep green tint behind brand chips/icons
val Accent = Color(0xFF8B7CF6)      // Electric indigo — secondary highlights

// Typography
val TextPrimary = Color(0xFFF8FAFC) // Crisp white
val TextMuted = Color(0xFF94A3B8)   // Muted slate
val TextLight = Color(0xFF5B6472)   // Placeholder / tertiary text

// Status badges (used in simple flat contexts, e.g. list badges)
val Danger = Color(0xFFF87171)
val DangerBg = Color(0xFF3F1717)

// Navigation
val NavUnselected = Color(0xFF7C8794)
val NavSelected = Color(0xFF10B981)

// (End of theme colors)